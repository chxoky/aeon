---
id: 002
title: twitterapi.io webhook stopped delivering — zero x-trader-monitor events since 2026-06-16
status: resolved
severity: high
category: api-change
detected_by: claude-code-session
detected_at: 2026-07-13
resolved_at: 2026-07-14
affected_skills: [x-trader-monitor]
root_cause: two-part — (1) twitterapi.io tweet-filter rule WATCHED_TRADERS was dead/deactivated (re-activated by Kyle 2026-07-13 ~18:55 PT); (2) Worker /twitter handler parsed the wrong payload shape — live API is camelCase (author.userName, createdAt, top-level media[]) and batches all window matches in tweets[], so every real tweet resolved to username "unknown" and was silently dropped; only tweets[0] was ever read
fix_pr: "#9 (payload shape), #10 (KV dedup for redeliveries)"
---

# twitterapi.io webhook stopped delivering

## Symptom

Zero `x-trader-monitor` runs on origin/main since 2026-06-16 (~27 days), while
`discord-trader-monitor` fires many times daily over the same period. 12 active
traders posting nothing for a month is not plausible — the X inbound path is dead.

## What was ruled out (2026-07-13 session)

- **Worker → GitHub → skill path works.** Synthetic POST to
  `https://trading-bot.chxoky.workers.dev/twitter` with a twitterapi.io-shaped
  payload dispatched `x-trader-monitor` within seconds; the skill classified the
  test tweet correctly (HR-1 silent skip) and auto-committed its log.
- **Username field mapping is not the cause this time.** The 2026-06-16 fix
  (`tweet.user.screen_name` fallback chain in `handleTwitter`) covers all known
  payload shapes; the previous same-symptom incident (ISS-less, fixed 06-16) was
  this mapping, but events then were at least REACHING the Worker.

## Root cause narrowed (2026-07-13, same session)

`trader-x-bootstrap force` run from the PR #5 branch (run 29299646760) fetched
**97 tweets from 10/12 watched accounts (2026-07-11 → 07-13)** via the new
secretcurl path using `TWITTERAPI_IO_KEY`. Therefore:

- API key valid, account balance fine, REST access working.
- Traders are actively posting (97 tweets in 3 days) while the webhook
  delivered ZERO events over the same window.

→ **The twitterapi.io webhook/tweet-filter RULE itself is dead** (expired,
deactivated, or deregistered). Not credits, not the Worker, not the field
mapping.

## Fix

Log into the twitterapi.io dashboard (or use the rules API with
`TWITTERAPI_IO_KEY` via `./secretcurl` post-merge): re-create/re-activate the
tweet-filter webhook rule for the 12 watched accounts pointing at
`https://trading-bot.chxoky.workers.dev/twitter`, then confirm one real tweet
event produces an x-trader-monitor run. Also worth checking whether rules
auto-expire — if so, add a rule-health check to `api-health-probe` or
`heartbeat` so a dead rule alerts within a day instead of a month.

## Fix applied (2026-07-13 ~18:55 PT)

Kyle re-registered/re-activated the rule via the twitterapi.io dashboard:

- Rule `WATCHED_TRADERS` (rule_id `35f3c57caf934741a13daf03a64c987b`), **ON_AIR**,
  checked every 180s, `from:` filter covering all 12 watched accounts.
- Dashboard confirms `webhook:tweet_filter` checks returning 200 every 3 min since
  ~18:55 PT (15 credits/check ≈ 7.2K credits/day; balance ~749K recharge credits).
- No matched tweets delivered yet (Data Count `—` on each check) — awaiting the
  first REAL tweet from a watched account to produce an `x-trader-monitor` run.

**To close:** confirm one real (non-synthetic) `x-trader-monitor` event run on
GitHub Actions, then set `status: resolved` + `resolved_at`. Follow-up hardening
(separate task): add a twitterapi.io rule-health check (`is_effect`/ON_AIR status
via rules API) to `heartbeat` so a dead rule alerts within a day, not a month.

## Resolution (2026-07-14)

Closed on first REAL x-trader-monitor run: trading_axe tweet 2076898161675702767,
run 29308762312 (success, 05:31 UTC). Wild_Randomness event followed minutes later.
Root cause was two-part: the dead rule (fixed 07-13 by Kyle) AND a Worker payload-shape
mismatch (fixed PR #9 — camelCase fields + batch iteration; verified live via synthetic
POST in real API shape and by real events flowing immediately after deploy). PR #10
added KV dedup because twitterapi.io redelivers each window's matches every 180s poll.
Guard: heartbeat rule-health check (PR #7) now runs on schedule again (scheduler
re-enabled Session F).
