# 2026-09-10

### cowork-session — TradingView execution layer (research → implement → deploy)
- session: ~12:50–14:25 PDT, Cowork, driven by Kyle. Handoff:
  `~/Claude/Projects/Telegram Bot Discord/X/handoffs/HANDOFF-2026-09-10.md`
- retro-logged 2026-09-10 (this entry was missed at session end; the session modified `~/aeon`
  and deployed, so the audit trail had a genuine gap — self-flagged in the handoff header)
- trigger: Kyle asked for a TradingView `ARCHITECTURE.md`; mid-session corrected the framing from
  a hardcoded BTC→BITX pair to a general rule engine (alerts on TV → orders on his brokerage,
  covering both managing open positions and starting new ones, equities + crypto)
- keystone findings (verified against primary sources, not assumed):
  - no alerts API exists in any of the three TradingView `.d.ts` specs; TV alerts+webhooks are a
    hosted product feature. The Broker API is an interface you IMPLEMENT, called by the library
    into your code. **TradingView is the eyes, never the hands.**
  - Kraken cannot execute the equities leg — no API for traditional US equities (UI-only),
    no extended hours at all, and xStocks is unavailable to US customers. Blocker is the API,
    not geography. Alpaca is the forced choice (and is the broker-dealer behind Kraken equities).
  - Kyle's existing approval invariant already splits the two cases: `kraken-execute` requires
    per-entry approval; `close-trade` documents a mirror-initiated no-approval exit path. The new
    engine was built to that existing boundary rather than inventing one.
- built: `worker/src/tv/{index,kraken,alpaca,rules,symbols}.js`, `worker/test/tv-alert.test.mjs`,
  `worker/TV-ALERTS.md`. +1,632 lines, 0 deletions. Additive edits to `worker/src/index.js` (+31)
  and `wrangler.toml` (+13, `RULE_STATE` Durable Object + `v1-rule-state` migration).
- routes: `POST /tv-alert`, `POST /tv-rule` (status/rearm), Telegram `go <ruleId>` / `deny <ruleId>`
- decisions locked: D1 Kraken=crypto / Alpaca=equities (forced) · D2 TV alerts as trigger for now ·
  D3 approval boundary mirrors existing invariant (exits auto, entries ask) · D4 Durable Object not
  KV (at-least-once delivery + no CAS in KV = real double-sell risk) · D5 strict symbol resolution,
  no heuristics · D6 execute in-Worker (~500ms) not via AEON skill (~2min) · D7 Alpaca defaults to
  PAPER, live requires `ALPACA_LIVE` exactly `"true"`
- bugs found and fixed before going live (5, all mine): `trim` by usd would have closed the ENTIRE
  position · `dry_run` bypassed on close/cancel paths · `pct` sizing on a new entry failed deep in
  the adapter · **`marketStatus` could never return `"post"`** (derived from the `next_open` gap;
  rewritten against the ET wall clock) · Telegram `go|deny` matched on shape alone and swallowed
  ordinary messages like "go check the charts"
- deployed: `https://trading-bot.chxoky.workers.dev`, 5 deploys, final version
  `9ba85e12-1800-434d-999a-45ba12ec2f5a`. Real brokerage credentials exercised against real APIs
  (read-only + validate-only). **No order was ever placed.** Kraken JS/Python signing parity
  confirmed byte-identical. 53 unit assertions passing. Telegram delivery confirmed by Kyle (9 msgs).
- safety posture verified four ways: unauthorized without the secret · no pending confirmations left
  in KV · cron handler runs `pollDiscord` only (no TV path, no timer, no self-trigger) · every order
  call reachable only from `handleTvAlert` (post-claim) or `resolvePending` (post-`go`).
  ⚠ unchanged pre-existing autonomous path: the `close-trade` mirror-initiated exit still manages
  positions without per-trade approval. That is not covered by "nothing can trade."
- ⚠ traps carried forward: Kraken nonce space is SHARED between `src/tv/kraken.js` and
  `scripts/postprocess-kraken.sh` — both must stay in **milliseconds** · Alpaca rejects extended-hours
  market orders (`extended_hours` requires `type=limit`) · `closePosition` without `percentage`/`qty`
  liquidates everything · session must be read at execution time, not trigger time · DO claim must
  stay before any side effect · the Worker now holds brokerage keys AND calls Anthropic — credential
  separation is a code-discipline invariant (`src/tv/{kraken,alpaca}.js` only), not a process boundary
- git: branch `feat/tradingview-execution`, commit `3e80ebad1`, pushed to `origin` 2026-09-10.
  **`main` is unchanged, and the live Worker runs this branch's code — `main` and production remain
  out of sync until this is merged and/or redeployed. `git checkout main` does not roll back a deploy.**
- open: `TV_WEBHOOK_SECRET` rotation (it is in the session transcript) · `pre` session detection
  unverified live · holiday edge in session detection (benign failure, deliberately deferred) ·
  Alpaca fractional-share order types untested · denied entries stay spent · rule authoring is manual
  (nothing connects `x-trader-monitor` / `discord-trader-monitor` to the engine yet)
- routing note: implementation was done directly against `~/aeon` from Cowork, departing from
  `CLAUDE.md` §3 (implementation → Claude Code). Kyle drove it that way; the convention should be
  updated or re-asserted deliberately.
