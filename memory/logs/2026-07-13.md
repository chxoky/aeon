# 2026-07-13

## [PT] Cowork — Aeon framework audit, drift cleanup, v0.1 gap analysis
- Audited fork vs upstream: chxoky/aeon is ~363 commits behind aaronjmars/aeon;
  last upstream sync 2026-06-08; v0.1.0 (2026-07-09) NOT adopted. Confirmed updates are
  never automatic (fork-and-own; scripts/sync-upstream.sh only syncs skills.json skills,
  not core framework).
- Key v0.1 gaps affecting this fork: secretcurl per-skill secrets (replaces our
  prefetch/postprocess security model), Telegram Instant mode (may replace Worker
  /telegram routing), attestation, OKF. Migration planned as a dedicated Claude Code
  session — see workspace handoffs/HANDOFF-2026-07-13.md.
- Workspace drift resolved: deleted stale skills/, scripts/, memory/ duplicates from the
  Cowork workspace (fork was newer on every file); workspace CLAUDE.md rewritten — fork
  is now the declared single source of truth; handoff protocol added (read newest
  handoffs/ file at start, write dated handoff at end).
- Security: plaintext gh OAuth token found in workspace HANDOFF-2026-06-16.md; redacted
  from file. Verified token is STILL ACTIVE (= current gh CLI token). Worker unaffected
  (uses fine-grained PAT since 06-16). Next: revoke GitHub CLI OAuth app, `gh auth login`.
- Reddit/community digest + adoption decisions saved to workspace
  reddit-digest-2026-07-13.md. Roadmap artifact: claude.ai/code/artifact/4185fb36.
- NOTE: this log entry is uncommitted — commit with the next fork session's work.

## Known-stale doc (fix in next fork session)
- memory/MEMORY.md still says GH Actions cron runs as monitor fallback; aeon.yml has
  monitors enabled:false intentionally (webhook/Worker-triggered). Update the line.
  → FIXED same day (Session B below).

## [PT] Claude Code — Session B: v0.1 secretcurl migration (branch v01-secretcurl-migration)
- Fetched upstream (aaronjmars/aeon) + v0.1.0 tag; analyzed secretcurl/per-skill-secrets
  model instead of blind-merging 363 commits.
- Adopted from v0.1.0: scripts/secretcurl.sh ({ENV} placeholder substitution inside the
  script — secret never on the analyzed command line) and scripts/skill_requires.sh
  (`requires:` frontmatter parser). Workflow Run step now injects ONLY a skill's declared
  keys from ALL_SECRETS (least privilege), then unsets the blob; ./secretcurl copied to
  repo root + allow-listed.
- Migrated bootstrap skills off prefetch: prefetch-trader{,-x}-bootstrap.sh →
  scripts/fetch-trader{,-x}-bootstrap.sh, invoked BY the skill via ./secretcurl;
  SKILL.md frontmatter now declares requires: [TWITTERAPI_IO_KEY(, DISCORD_USER_TOKEN)].
- Deleted dead prefetch-x-traders.sh + prefetch-discord-traders.sh: monitors are Worker
  event-driven (${var}) since 06-16 and never read the cache — those scripts were
  re-fetching 24h of X/Discord history on EVERY real-time event (pure API-quota waste).
- Kept postprocess for kraken (HMAC-SHA512 signing) and prefetch for chart-request/xai/
  api-probe/fleet-scorecard/liquidpad (legacy; documented in CLAUDE.md sandbox section).
- Telegram Instant mode (v0.1): evaluated, SKIP — our Worker /telegram is already
  instant, has the KV-backed approve→confirm trade state machine and direct Claude
  freeform answers; upstream's router is generic, LLM-less, still GH-Actions-latency,
  and has no approval flow. Optional future borrow: inline-button callbacks + snooze/mute.
- memory/MEMORY.md cron-fallback line corrected (monitors: no cron fallback, intentional).
- Verified locally: secretcurl substitution live against httpbin (200, value substituted),
  skill_requires parses both new frontmatters, bash -n clean, workflow YAML lint clean.
